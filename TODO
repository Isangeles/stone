MAJOR:
* Right now map.Draw draws whole map, that is unnecessary, only visible part of map(i.e. in passed Pixel matrix) should be rendered
MINOR:
* Example of map with movable camera
* map.tileBounds function looks awful, should be rewritten to be shorter/more understable